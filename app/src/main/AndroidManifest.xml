<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络事件诊断（NetworkEventLog）：监听默认网络的丢失/切换/IP 变更。安装时权限，无弹窗。 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- M11.1：FSShell SDK 底层用 libusb 操作 usbfs，部分严格系统需此权限访问 /dev/bus/usb（非必需项）。 -->
    <uses-permission android:name="android.permission.HARDWARE_TEST" />
    <!--
      会话期间按住 CPU（PARTIAL_WAKE_LOCK，见 SessionKeepAlive）：屏一灭 CPU 睡下去，心跳协程不再按时醒、
      对端的帧也没人读——睡着那端醒来有宽限不会误杀自己，但对端没睡，数满 60 秒静默就判死，一台息屏拖着两台
      一起掉线。安装时权限、无弹窗，仅在会话存续期间持有，且带 10 分钟兜底超时（由心跳续期）。
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
      会话期间的前台服务（见 SessionForegroundService）：2026-08-01 日志证实，用户一开系统文件选取器或一锁屏，
      App 被判后台、系统当场断其网，socket 被本机协议栈中止。前台服务是唯一能让进程不被判后台的正规手段。
      三条均为安装时权限、无弹窗；DATA_SYNC 只作 connectedDevice 被 ROM 判不过时的退路。
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:name=".MiDunApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiDun">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MiDun"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <!-- 会话保活。不导出：只由本进程按会话状态起停，不提供任何 IPC 面。 -->
        <service
            android:name=".service.SessionForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <!-- 分享二维码图片用：把 cache 下的 PNG 经内容 URI 授权给分享目标。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>